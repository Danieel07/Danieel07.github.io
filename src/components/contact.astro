<section id="contact" class="w-full py-12 border-t border-[#ffffff10]">
  <div class="max-w-5xl mx-auto">
    <h2 class="text-lg text-[var(--sec)] mb-2 shiny-sec" data-i18n="contact.badge">Hablemos</h2>
    <h3 class="text-4xl md:text-5xl font-medium text-[var(--white)] mb-6" data-i18n="contact.title">
      Contacto
    </h3>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      <div class="text-[var(--white-icon)]">
        <p class="mb-4 leading-relaxed" data-i18n="contact.subtitle">
          ¿Tienes alguna pregunta o proyecto en mente? No dudes en escribirme.
        </p>
        <div class="flex items-center gap-2">
          <span data-i18n="contact.locationLabel">Ubicación:</span>
          <span class="text-[var(--white)] font-medium">Colombia</span>
        </div>
      </div>

      <div>
        <form
          id="contact-form"
          action="https://formspree.io/f/YOUR_FORM_ID_HERE"
          method="POST"
          class="flex flex-col gap-4"
        >
          <input
            type="text"
            name="from_name"
            placeholder="Nombre"
            data-i18n-placeholder="contact.namePlaceholder"
            required
            class="px-4 py-2 bg-[#1414149c] text-[var(--white)] border border-[var(--white-icon-tr)] rounded-lg focus:outline-none focus:ring-2 focus:ring-[var(--sec)]"
          />
          <input
            type="email"
            name="reply_to"
            placeholder="Correo electrónico"
            data-i18n-placeholder="contact.emailPlaceholder"
            required
            class="px-4 py-2 bg-[#1414149c] text-[var(--white)] border border-[var(--white-icon-tr)] rounded-lg focus:outline-none focus:ring-2 focus:ring-[var(--sec)]"
          />
          <textarea
            name="message"
            placeholder="Mensaje"
            data-i18n-placeholder="contact.messagePlaceholder"
            rows="6"
            required
            class="px-4 py-2 bg-[#1414149c] text-[var(--white)] border border-[var(--white-icon-tr)] rounded-lg focus:outline-none focus:ring-2 focus:ring-[var(--sec)] resize-none"
          ></textarea>
          <button
            type="submit"
            data-i18n="contact.submitButton"
            class="px-4 py-2 bg-[var(--white-icon-tr)] text-[var(--white)] rounded-lg opacity-80 transition-opacity border border-[var(--white-icon-tr)] hover:opacity-100 hover:bg-[var(--white-icon-tr)] font-medium"
          >
            Enviar
          </button>
        </form>
        <div
          id="form-message"
          data-i18n="contact.successMessage"
          class="hidden justify-center items-center mt-4 text-[var(--white)] text-lg"
        >
          ✅ ¡Gracias por tu mensaje!
        </div>
      </div>
    </div>
  </div>
</section>

<script type="module" is:inline>
  const form = document.getElementById("contact-form");
  const formMessage = document.getElementById("form-message");

  form.addEventListener("submit", async (e) => {
    e.preventDefault();
    const formData = new FormData(form);

    try {
      const response = await fetch(form.action, {
        method: "POST",
        body: formData,
        headers: { Accept: "application/json" },
      });
      if (response.ok) {
        form.reset();
        form.style.display = "none";
        formMessage.classList.remove("hidden");
      } else {
        const data = await response.json();
        console.error("Error response:", data);
        alert("There was a problem sending your message.");
      }
    } catch (error) {
      console.error("Error:", error);
      alert("There was a problem sending your message.");
    }
  });
</script>
